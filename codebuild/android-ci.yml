version: 0.2

phases:
  build:
    commands:
      - cd android
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
    artifacts:
      files:
        - './app/build/outputs/**/*.apk'
      discard-paths: yes